let name = prompt("What is your pet's name?");
console.log(name);

let answer = prompt("Did you feed, pet or walk him/her?");
console.log(answer);

for (let i = 0; i < 5; i++) {
    console.log('answer' + i);
    if(i === 0) break;
    if(answer === feed )
}